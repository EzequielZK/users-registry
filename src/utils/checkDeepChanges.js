export default function checkArrayDeepChanges(pastArray = [], newArray = []){
    if(pastArray.length !== newArray.length){
        return true;
    }
    else{
        let i = 0;
        const pastLength = pastArray.length;
        const newLength = newArray.length;

        for(; i < pastLength; i++){
            
        }
    }
}

function checkObjectDeepChanges()